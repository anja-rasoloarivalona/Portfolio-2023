import { box } from './box';
import { colors } from './colors';
import { font } from './font';
import { config } from './config';

const theme = {
    font: font,
    box: box,
    colors: colors,
    config: config,
};

export default theme;
